# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Subito' do
  pod 'Kingfisher', '~> 7.0'

  target 'SubitoTests' do
    inherit! :search_paths
  end
end
